# App
NODE_ENV=development

# LLM Extractor (For extracting atoms from papers - fast model recommended)
EXTRACTOR_API_KEY=your_api_key_here
EXTRACTOR_BASE_URL=https://api.openai.com/v1
EXTRACTOR_MODEL_NAME=gpt-3.5-turbo

# LLM Synthesizer (For generating ideas - smart model recommended)
SYNTHESIZER_API_KEY=your_api_key_here
SYNTHESIZER_BASE_URL=https://api.openai.com/v1
SYNTHESIZER_MODEL_NAME=gpt-4

# Supabase (Database & Auth) - REQUIRED for database and vector search
# Even for local development, this project uses Supabase as the PostgreSQL + pgvector provider.
# You can use a free cloud project at supabase.com or run a local instance via Docker.
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Cron (Optional, for scheduled tasks security)
CRON_SECRET=my-secret-cron-key

# Frontend (Vite)
# NOTE: Must match Supabase credentials above
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Dev Mode (Set to true to skip login requirement)
VITE_SKIP_AUTH=true
